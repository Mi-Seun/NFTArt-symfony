{% extends 'base.html.twig' %}

{% block title %}New Nft{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header text-center">
                <h1>Create new Nft</h1>
            </div>
            <div class="card-body">
                {{ include('nft/_form.html.twig') }}
            </div>
        </div>
        <a href="{{ path('app_nft_index') }}" class="btn btn-secondary mt-3">Back to List</a>
    </div>
{% endblock %}
