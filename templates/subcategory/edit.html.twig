{% extends 'base.html.twig' %}

{% block title %}Edit Subcategory{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Edit Subcategory</h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ include('subcategory/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
        <div class="row">
            <div class="col">
                <a href="{{ path('app_subcategory_index') }}" class="btn btn-secondary">Back to List</a>
            </div>
            <div class="col">
                {{ include('subcategory/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
