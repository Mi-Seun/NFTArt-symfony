{% extends 'base.html.twig' %}

{% block title %}Edit Nft{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header text-center">
                <h1>Edit Nft</h1>
            </div>
            <div class="card-body">
                {{ include('nft/_form.html.twig',  {'button_label': 'Update'}) }}
            </div>
        </div>
        <a href="{{ path('app_nft_index') }}" class="btn btn-secondary mt-3 m-1">Back to List</a>
        {{ include('nft/_delete_form.html.twig') }}
    </div>
{% endblock %}

